version: '3.8'

services:
  database:
    image: postgres:latest
    ports:
      - 5432:5432
    environment:
      - POSTGRES_DB=sdc
      - POSTGRES_PASSWORD=pass
      - POSTGRES_USER=user

  server:
    build: ./backend
    ports:
      - 8000:8000
    environment:
      - connectionString=postgresql://user:pass@database:5432/sdc
      - PORT=8000
    depends_on:
      - database
